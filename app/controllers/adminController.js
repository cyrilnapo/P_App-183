const crypto = require('crypto');
const userModel = require('../models/userModel');

exports.searchUsers = (req, res) => {
  const { name } = req.query;
  // Code pour rechercher les utilisateurs dans la base de données
};
